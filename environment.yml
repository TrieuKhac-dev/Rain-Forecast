name: rain-forecast
channels:
  - conda-forge
  - pytorch
  - defaults

dependencies:
  - python=3.10.*
  - pip>=21.3
  - mypy>=1.10,<2.0
  - ruff>=0.6,<1.0
  - pre-commit>=3.0,<5.0.0
  - pandas>=1.3,<3.1.0
  # Khai báo gói PyPI tại đây.
  - pip:
      - clearml>=1.13.0,<=1.15.3

name: rain-forecast
channels:
  - conda-forge
  - pytorch
  - defaults

dependencies:
  - python=3.10.*
  - pip>=21.3
  - mypy>=1.10,<2.0
  - ruff>=0.6,<1.0
  - pre-commit>=3.0,<4.0
  - pandas>=1.3,<2.3
  # Khai báo gói PyPI tại đây.
  - pip: []
    # Ví dụ:
    # - pip:
    #     - requests>=2.32.0
    #     - python-dotenv>=1.0.1
